#!/usr/bin/env ruby
$LOAD_PATH.unshift(File.join(File.dirname(__FILE__), '..', 'lib'))

require 'ralf'

begin
  if options = Ralf.parse_options(ARGV)
    Ralf.run(options)
  end
rescue => e
  STDERR.puts "Error: #{e}"
end